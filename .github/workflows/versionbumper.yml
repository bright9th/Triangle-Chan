name: Auto bump version in PR branch

on:
  pull_request:
    types: [opened, synchronize]
    branches:
      - main

jobs:
  bump-version:
    runs-on: ubuntu-latest

    if: github.event.pull_request.head.repo.full_name == github.repository &&
      github.actor != 'dependabot[bot]' &&
      github.actor != 'github-actions[bot]'

    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"

      - name: Bump version
        id: bump
        run: node .github/scripts/bump-version.js

      - name: Set Git Identity
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Push changes back to PR branch
        run: |
          git pull origin ${{ github.head_ref }}
          git add package.json
          git add package-lock.json
          git add README.md
          git add .github/vbump-config.json
          git commit -m "Version Update [ v${{ steps.bump.outputs.newVersion }} ]" || echo "No changes to commit"
          git push origin HEAD:${{ github.head_ref }}
